services:
  trino:
    image: trinodb/trino:479
    container_name: trino
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./catalog:/etc/trino/catalog:ro
    depends_on:
      - mongodb

volumes:
  mongo_data:
